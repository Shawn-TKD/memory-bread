# 记忆面包开源发布清单

这份清单用于维护一个独立、可复现且不会泄露个人凭据的 GitHub 仓库。当前 `origin` 指向 `Shawn-TKD/memory-bread`；原始代码来源单独保留为只读参考用的 `upstream`。

## 1. 推荐仓库定位

推荐仓库名：

- `memory-bread`：简洁，适合产品和公众号统一使用。
- `memory-bread-esp32`：更容易让开发者一眼看出硬件平台。
- `jiyi-mianbao`：中文拼音辨识度较高，但海外开发者不容易理解。

优先推荐 **`memory-bread`**，中文标题使用“记忆面包：开源墨水屏 AI 语音笔记”。

仓库说明可以写成：

> 基于微雪 ESP32-S3 1.54 英寸墨水屏开发板的离线优先语音笔记设备，支持中文 UI、长录音、硅基流动 ASR/DeepSeek 整理和闪念贝壳 MCP 同步。

## 2. 发布前必须处理的凭据

- 在硅基流动后台撤销并重建所有曾经出现在聊天、截图或演示视频中的 API Key。
- 在闪念贝壳“设置 → MCP → API 密钥”重置所有曾经公开过的 Token。
- 确认 `memory_bread/secrets.h` 只有 `....` 占位符。
- 搜索源码、Markdown、串口日志和截图中是否出现 `sk-`、`Bearer`、Wi-Fi 密码、GitHub Token。
- 不要提交设备 NVS 导出、microSD 卡内容、串口日志、coredump、设备 MAC 地址或家庭/办公室 SSID。

密钥已经从 Git 提交中删除，并不代表它从 Git 历史中消失；只要曾经提交或公开，就应该在服务商后台撤销。

## 3. 许可证与来源说明

当前仓库根目录包含上游代码沿用的 MIT `LICENSE`。发布自己的分支时：

- 保留原 MIT 许可证和版权声明。
- 在 `ACKNOWLEDGEMENTS.md` 中保留上游代码和原始硬件项目的来源链接与贡献说明。
- 单独列出“记忆面包新增内容”，例如中文 UI、中文字库、三组 Wi-Fi、手机配网、长录音、提示音主题、硅基流动和闪念贝壳 MCP。
- 3D 外壳文件若来自原 Pala Note 下载包，不要在没有明确再分发许可的情况下复制进公开仓库；可以保留原项目购买或下载链接。
- 第三方字体、图片、音频和库要逐项保留来源与许可证。中文字体尤其需要在发布前确认字体源允许嵌入和再分发。

这是一份发布检查建议，不替代正式法律意见。

## 4. 建议仓库结构

```text
memory-bread/
├── README.md                 # 项目介绍、照片、能力边界、快速开始
├── USER_GUIDE_ZH.md          # 面向普通用户的完整说明书
├── HARDWARE_ZH.md            # 板卡型号、接线、电池、外壳和采购清单
├── BUILD_ZH.md               # arduino-cli 安装、编译和 USB 烧录
├── CHANGELOG.md              # 每个固件版本的变化
├── SECURITY.md               # 密钥、漏洞报告与隐私说明
├── LICENSE
├── memory_bread/             # Arduino 固件源码
├── tools/                    # 字库生成等开发工具
└── docs/images/              # 不包含密钥、SSID 和个人笔记的产品图片
```

编译产物不应直接散落在源码提交里。正式发布固件时，可以在 GitHub Release 单独上传：

- `memory-bread-v1.5.bin`：用于网页 OTA 的应用固件。
- `memory-bread-v1.5-merged.bin`：用于救砖或首次完整烧录的合并镜像。
- SHA-256 校验值。
- 版本变更、兼容板型和升级注意事项。

## 5. README 发布检查

README 已改为“记忆面包”的独立产品首页。后续发布时继续检查：

1. 产品名称和一张不含隐私信息的实拍图。
2. 明确支持的开发板型号：微雪 ESP32-S3-ePaper-1.54 黑白屏 N8R8。
3. 当前真实链路：本地 WAV → 硅基流动 ASR → DeepSeek 整理 → 闪念贝壳 MCP。
4. 强调录音默认离线保存、同步由用户手动触发。
5. 写清楚长录音、三组 Wi-Fi、中文 UI、OTA 与存储限制。
6. 把 GitHub/Obsidian 标为旧版兼容功能，或者等源码真正删除后从文档移除。
7. 链接 `USER_GUIDE_ZH.md`、构建文档、许可证和致谢文件。

## 6. 建议的 Git 远程关系

新仓库创建后，建议保留上游来源，形成：

```text
origin    你的账号/memory-bread
upstream  原始代码仓库
```

不要直接覆盖或强推上游仓库。第一次发布前先检查工作区中所有修改，将固件、文档、生成字库和调试工具分成有意义的提交；不要把 `.build/`、个人配置或测试录音加入提交。

## 7. 公众号发布建议

公众号文章可以分成三层：

1. **产品故事**：为什么做“记忆面包”，它解决什么场景。
2. **十分钟上手**：购买同款板卡、准备 microSD、烧录、配 Wi-Fi 和两枚 Token。
3. **开发者入口**：GitHub 地址、架构图、编译步骤、Issue 反馈和路线图。

不要在正文截图中展示真实 Key、Token、家庭 Wi-Fi、设备 IP 或个人笔记。演示账号应使用可随时撤销的临时凭据，发布后立即轮换。

## 8. 创建远程仓库前的最终确认

- 仓库名称与 GitHub 账号/组织。
- 保持公开仓库还是先建私有仓库检查一轮。
- 是否继续使用 MIT 许可证。
- 是否暂时保留旧版 GitHub/Obsidian 源码。
- 中文字库已切换为基于思源黑体 CN Regular 生成的 Memory Bread CJK Bitmap，并附带 SIL OFL 1.1；发布前确认 `THIRD_PARTY_NOTICES.md` 与 `licenses/OFL-1.1.txt` 一同提交。
- 公众号首发使用 v1.5，还是等长录音再完成一次 5～10 分钟实机验证后发布。

推荐流程是先创建**私有**的 `memory-bread`，完成一次密钥扫描和全新设备安装验证，再切换为公开仓库。
