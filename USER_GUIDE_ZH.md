# 记忆面包使用说明书

“记忆面包”是一款基于微雪 ESP32-S3-ePaper-1.54 开发板的离线优先语音笔记设备。它可以在不连接手机和网络的情况下录音并保存到 microSD 卡；需要时再连接 Wi-Fi，批量完成语音转写、AI 整理和闪念贝壳同步。

本文档适用于当前已经烧录的 **v1.5-memorybread** 固件。

### v1.5 本次更新

- 修复长录音超过 2 分钟后，结束录音时被待机计时器立即送入深度睡眠的问题。
- WAV 关闭成功后立即写入笔记索引；标签页面只负责修改分类，不再决定录音是否可见。
- 开机时自动扫描并恢复“文件已保存、但索引缺失”的 WAV，恢复项默认归入“随记”。
- 新笔记编号同时检查索引和 microSD 卡中的实际文件，避免覆盖未被索引的录音。
- 继续包含 v1.4 的笔记浏览响应、墨水屏后台刷新、文字缓存、附近 Wi-Fi 扫描和手机端配网页面优化。

### v1.4 已包含的改进

- 优化笔记列表、笔记详情和翻页过程中的按键响应。
- 墨水屏刷新改为后台合并处理；画面尚未刷完时，按键仍会继续被检测。
- 缓存笔记标题和转写文本，减少浏览时反复读取 microSD 卡造成的卡顿。
- 配网页面新增附近 2.4 GHz Wi-Fi 扫描和点选填入功能。
- 配网页面改为手机优先布局，分开显示三组 Wi-Fi、AI 转写和同步服务。
- 飞书同步目前只显示“规划中”；闪念贝壳是当前可直接使用的主要同步服务。

## 1. 适用硬件

- ESP32-S3-PICO-1，双核 240 MHz
- 8 MB Flash、8 MB PSRAM
- 1.54 英寸电子墨水屏，分辨率 200 × 200
- microSD 卡，建议使用 FAT32 格式
- 板载麦克风、扬声器和音频编解码芯片
- 板载锂电池充放电管理
- BOOT 与 PWR 两个侧边按键
- 2.4 GHz Wi-Fi 与 Bluetooth LE

当前固件使用按键操作，不使用触摸功能。即使购买触控版开发板，触控屏在这一版固件里也暂时不会响应。

## 2. 两个按键

| 按键 | 正常作用 | 菜单中的作用 |
|---|---|---|
| 录音键（BOOT / GPIO0） | 长按碎片录音；亮屏时连续短按三次进入长录音 | 短按确认，长按返回 |
| 电源键（PWR / GPIO18） | 从待机进入菜单 | 短按移动到下一项，部分页面长按执行次要操作 |

按键长按判定约为 0.45 秒；待机录音在按住约 0.35 秒后开始。

录音键同时是开发板的 BOOT 键。只有烧录固件时才需要在上电前按住它，日常录音不需要重新插 USB。

## 3. 快速开始

1. 插入 FAT32 格式的 microSD 卡。
2. 正常开机后，屏幕显示“记忆面包”。
3. 按住录音键约 0.35 秒，看到黑底白色圆形动画后开始说话。
4. 保持按住录音键；松开后结束并保存录音。
5. WAV 关闭成功后会立即加入笔记列表，然后进入标签选择页面。
6. 短按电源键切换标签，短按录音键确认。
7. 确认标签后，设备自动进入低功耗待机。

如果在标签页面断电、重启或因等待过久进入睡眠，录音仍然已经保存；下次开机时还会自动恢复极少数“有 WAV、无索引”的遗留录音。

碎片录音至少约 0.5 秒，最长 5 分钟。录音格式为 16 kHz、16 位、单声道 WAV。

### 长录音

- 设备亮屏待机时，连续短按录音键三次；也可以进入“菜单 → 长录音”。
- 屏幕显示“长录音”和计时后开始讲话。
- 再短按一次录音键，结束并保存。
- 一次长录音只生成一个完整 WAV，不会按分钟拆成多条笔记。
- 长录音安全上限为 12 小时；空间不足或电量降至约 5% 时会自动结束并保存。
- 固件每约 30 秒更新一次 WAV 文件头，降低意外掉电导致整段文件无法播放的风险。

三击只在设备已经亮屏时识别。设备休眠时可先按电源键进入菜单，再选择“长录音”；睡眠时按住录音键仍会直接进入碎片录音。

没有 Wi-Fi、API Key 或闪念贝壳 Token 时，录音功能仍可正常使用，WAV 文件会保存在 microSD 卡中。

## 4. 主菜单

在待机画面短按或长按电源键进入菜单。菜单包含：

1. **笔记**：浏览已经录制的内容。
2. **标签**：按标签筛选笔记。
3. **同步**：连接 Wi-Fi，批量转写和上传尚未处理的录音。
4. **长录音**：持续录音，再短按一次录音键结束。
5. **设置**：提示音、网页传输、设备信息、删除和重置。

菜单中短按电源键移动光标，短按录音键进入，长按录音键返回待机。

## 5. 浏览和播放笔记

### 笔记列表

- 短按电源键：选择下一条笔记。
- 短按录音键：打开当前笔记。
- 长按录音键：返回主菜单。

未完成语音转写的笔记仍可看到录音编号和标签；同步生成文字后，详情页会显示转写内容。

电子墨水屏本身的刷新速度比手机屏幕慢，按键后画面可能需要短暂刷新。v1.5 已将刷新与按键检测解耦，不需要重复按键；正常短按并松开，等待画面完成即可。为了避免触发“长按返回”，不要一直按住录音键等待屏幕变化。

### 笔记详情

- 短按电源键：显示下一页文字；到达最后一页后切换到下一条笔记。
- 短按录音键：播放当前 WAV 录音。
- 播放过程中按一下录音键：停止播放。
- 长按录音键：返回笔记列表。
- 长按电源键：进入删除确认页面。

当前录音播放音量固定为 85。设置中的提示音主题只控制按键、保存、成功和删除等操作反馈，不控制录音播放音量。

## 6. 标签

默认标签为：随记、工作、想法、购买、私密。

- 录音保存后，短按电源键切换标签，短按录音键确认。
- 在“菜单 → 标签”中，短按电源键切换标签，短按录音键查看该标签下的笔记。
- 长按录音键返回上一级。
- 可以在网页管理端添加或删除自定义标签，最多支持 20 个。

## 7. 配置 Wi-Fi 和服务

进入“菜单 → 设置 → 传输”。设备会尝试连接已经保存的 Wi-Fi：

- 连接成功：屏幕显示一个局域网 IP 地址，例如 `192.168.1.23`。
- 尚未配置 Wi-Fi，或三组 Wi-Fi 都无法连接：设备创建开放热点 `MemoryBread-Setup`。

### 使用配置热点

1. 用手机或电脑连接 `MemoryBread-Setup`。
2. 系统通常会自动弹出配置页。
3. 如果没有自动弹出，在浏览器访问 `http://192.168.4.1/provision`。
4. 页面会自动扫描附近的 2.4GHz Wi-Fi；先点选“网络 1/2/3”的名称输入框，再点扫描结果即可自动填入 SSID。
5. 如果没有显示网络，点击“重新扫描”。扫描期间手机仍需保持连接 `MemoryBread-Setup`。

扫描结果只负责填写网络名称，Wi-Fi 密码仍需手动输入。隐藏 SSID 不会出现在扫描结果中，但可以直接在名称输入框内手动填写。

### 使用家庭或办公室 Wi-Fi

1. 确保手机或电脑与记忆面包处于同一局域网。
2. 在浏览器打开设备屏幕显示的 IP。
3. 点击网页上方的“配置”。

设备最多保存 3 组 2.4 GHz Wi-Fi，例如家庭、办公室和手机热点。同步时优先尝试上次成功连接的网络，然后依次尝试其他网络。ESP32-S3 不支持连接 5 GHz Wi-Fi。

配置页密码框留空时，会保留同名网络已经保存的密码。勾选“删除这组网络”可以清除对应网络。

配置页以手机端为主要布局，依次分为 Wi-Fi、AI 转写和同步服务。闪念贝壳可以直接配置；飞书位置目前仅作功能预告，尚未启用；旧版 GitHub / Obsidian 设置收纳在页面底部的折叠区域，默认不需要填写。

保存后如果设备没有立即连上网络，请检查：网络是否为 2.4 GHz、密码是否正确、信号是否足够，以及企业网络是否要求网页认证。需要网页登录、短信验证或 802.1X 账号认证的网络，当前固件不能直接连接，建议改用普通家庭 Wi-Fi 或手机热点。

## 8. 硅基流动 API Key

这里的服务名称是“硅基流动（SiliconFlow）”，不是“国际流动”。语音转写和 AI 整理共用一枚硅基流动 API Key；Key 通常以 `sk-` 开头。

### 8.1 注册并创建 Key

1. 打开[硅基流动官网](https://siliconflow.cn/)，注册或登录账号。
2. 进入控制台，在左侧找到“API 密钥”或“账户管理 → API Key”。
3. 点击“新建 API 密钥”。可以将名称写成“记忆面包”，方便以后单独撤销。
4. 创建后立即复制并妥善保存。不要把它放进公众号文章、GitHub、截图或群聊。
5. 确认账号有可用余额或赠送额度；没有可用额度时，设备虽然能联网，但转写请求仍会失败。

硅基流动当前的 API 根地址是 `https://api.siliconflow.cn/v1`。本固件使用其[音频转写接口](https://docs.siliconflow.cn/cn/api-reference/audio/create-audio-transcriptions)和聊天模型接口。

### 8.2 写入设备

1. 在设备进入“菜单 → 设置 → 传输”。
2. 如果设备连上了家庭/办公室 Wi-Fi，用手机连接同一 Wi-Fi，再打开屏幕显示的局域网 IP。
3. 如果设备没有连上 Wi-Fi，用手机连接热点 `MemoryBread-Setup`，打开 `http://192.168.4.1/provision`。
4. 在“AI 转写”卡片中找到“硅基流动 API Key”。
5. 粘贴完整的 `sk-...`，不要带引号、空格或换行。
6. 点击页面底部“保存全部设置”，等待设备保存配置。

以后再次打开配置页时，Key 不会明文回显。密码框留空并保存表示保留原 Key，不是删除。若要更换 Key，直接填写新 Key 后保存；恢复出厂设置会清除设备中的 Key。

### 8.3 验证 Key 是否可用

当前配置页没有单独的“测试硅基流动”按钮，最可靠的测试方法是：

1. 先录一条 5～10 秒的测试录音。
2. 确认设备已经连上可访问互联网的 Wi-Fi。
3. 进入“菜单 → 同步”。
4. 同步结束后进入该笔记详情，检查是否已经出现转写文字。
5. 也可以进入网页管理端，查看是否生成对应的 `.txt` 内容。

如果“闪念贝壳测试连接”成功，只代表 Wi-Fi 和闪念贝壳 Token 正常，不能替代硅基流动 Key 的转写测试。

当前调用链路为：

```text
microSD WAV
  → 硅基流动 FunAudioLLM/SenseVoiceSmall 语音识别
  → 本地保存转写 TXT
  → 硅基流动 deepseek-ai/DeepSeek-V3.2 整理标题、摘要和正文
```

API Key 保存在设备的 NVS 配置区，不需要写入公开源码。不要使用已经出现在聊天记录、截图或公开仓库中的正式 Key；发现泄露后应立即在服务商后台撤销并重新生成。

### 8.4 常见错误

| 现象 | 常见原因 | 处理方法 |
|---|---|---|
| 录音存在，但同步后没有文字 | Key 未填写、复制不完整或余额不足 | 重新填写 Key，检查硅基流动控制台余额后再同步 |
| 返回 401 / 认证失败 | Key 错误、已撤销或带有多余字符 | 新建 Key，完整复制，不要加 `Bearer` 字样 |
| 返回 429 / 请求频繁 | 服务限流 | 等待一段时间后再次手动同步 |
| 长时间停在转写中 | Wi-Fi 弱、服务超时或音频较大 | 靠近路由器，先用短录音验证，再重试 |
| 闪念贝壳有旧笔记但没有新笔记 | ASR 未先生成文字 | 先解决硅基流动转写，再重试闪念贝壳同步 |

## 9. 闪念贝壳 MCP

### 9.1 MCP、OAuth 和 Bearer Token 的区别

闪念贝壳提供的 MCP 地址为：

```text
https://api.ideashell.cn/ideashell/mcp
```

在 ChatGPT 中创建连接时，可以选择 OAuth，由浏览器跳转到闪念贝壳完成登录授权。记忆面包是无浏览器的 ESP32 设备，当前固件不走这套交互式 OAuth，而是使用闪念贝壳生成的 API 密钥作为 Bearer Token。两种方式连接的是同一个 MCP 服务，但认证流程不同。

### 9.2 获取闪念贝壳 Token

按当前闪念贝壳界面操作：

1. 在手机或电脑上登录闪念贝壳。
2. 打开“设置”。
3. 进入左侧的“MCP”。
4. 找到“API 密钥”。如果尚未创建，先创建；如果已经存在，点击复制。
5. 将复制的整串密钥暂存到密码管理器，准备写入设备。

这个 Token 相当于账号访问凭据。别人拿到后可能读取或创建你的笔记，因此不要把它写进源码、公众号文章或公开 GitHub。怀疑泄露时，在闪念贝壳 MCP 页面点击“重置”，然后把新 Token 重新写入设备。界面名称以后如果有调整，以“设置 / MCP / API 密钥”这一条路径为准。

### 9.3 写入设备并测试

1. 进入设备“菜单 → 设置 → 传输”，用手机打开配置页。
2. 滚动到“同步服务 → 闪念贝壳 MCP”。
3. 在“Bearer Token”输入框粘贴刚复制的密钥。只粘贴密钥本身，不要手动添加 `Bearer ` 前缀。
4. 勾选“转写后自动写入闪念贝壳”。这里的“自动”表示每次手动执行同步时自动处理待上传笔记，不表示录音一结束就立即联网。
5. 点击“保存全部设置”。
6. 保持设备联网，在配置页点击“测试连接”。
7. 看到成功提示后，返回设备菜单，用一条已经转写出文字的测试笔记执行“同步”。

连接测试会完成 MCP 初始化、Bearer 认证，并调用只读的 `recent_notes(limit=1)`；它不会在闪念贝壳里创建测试笔记。正式写入时才调用 `note_create`。

### 9.4 实际同步过程

闪念贝壳 MCP 当前接收文字笔记，不直接接收设备录制的 WAV。因此实际链路是：

```text
录音 → 硅基流动 ASR → DeepSeek 整理 → 闪念贝壳 MCP note_create
```

设备只处理已经生成转写文字、且还没有闪念贝壳成功标记的笔记：

1. 读取 microSD 卡中的转写 TXT；闪念贝壳流程当前最多读取前 20,000 字节。
2. 使用 `deepseek-ai/DeepSeek-V3.2` 生成 2～8 个汉字的标题、一句话摘要和整理后的正文；当前送入整理模型的是前 6,000 字节。
3. 调用闪念贝壳 MCP 的 `note_create`，发送标题、正文、摘要和设备标签。
4. 写入成功后，在 microSD 卡生成同编号的 `.idea` 去重标记。
5. 下次同步看到该标记后会跳过这条笔记，避免重复创建。

原始 WAV 不会上传给闪念贝壳，仍留在 microSD 卡。整理后的正文会附带折叠的原始转写，便于核对。同步失败不会删除 WAV 或 TXT，也不会写成功标记，下次执行“同步”时可以继续重试。

这里的限制按 UTF-8 字节计算，不等于汉字数量。很长的会议录音虽然可以完整保存并完成 ASR，但当前闪念贝壳标题、摘要和整理正文主要基于前一部分转写，附带的原始转写也最多取前 20,000 字节。要把长录音用于完整会议总结，后续版本还需要加入转写文本分段、逐段总结和最终合并。

### 9.5 当前支持范围

闪念贝壳 MCP 还提供搜索笔记、最近笔记、笔记详情、更新笔记、人物检索和待办等工具；当前设备固件只使用：

- `recent_notes`：连接测试，只读最近一条笔记。
- `note_create`：正式同步，创建一条新笔记。

因此，现在是“设备写入闪念贝壳”，尚未实现“从闪念贝壳搜索并读取到墨水屏”或“在设备上管理待办”。这些能力可以作为后续版本加入，但需要额外设计语音意图识别、结果分页、隐私确认和失败回退。

### 9.6 闪念贝壳故障排查

| 现象 | 判断 | 处理方法 |
|---|---|---|
| 测试连接提示未填写 Token | 设备没有保存密钥 | 重新填写并点击“保存全部设置” |
| 测试提示认证失败 / HTTP 401 | Token 无效或已经重置 | 从闪念贝壳重新复制或重置 Token |
| 测试成功但没有新笔记 | 测试是只读的，不会创建内容 | 录制、完成 ASR，然后进入设备“同步” |
| 同步后仍无新笔记 | 没有 TXT、开关未勾选或服务请求失败 | 先确认本地已有转写文字，再检查开关并重试 |
| 同一条笔记不再上传 | 已存在 `.idea` 成功标记 | 这是防重复机制；不要随意删除标记 |
| 换了闪念贝壳账号仍跳过旧笔记 | microSD 上仍保留旧账号的 `.idea` 标记 | 当前版本不会自动跨账号重传；需在后续工具中增加“重新同步”功能 |

## 10. 同步机制

当前版本使用**手动批量同步**：

- 录音结束后不会立即联网上传。
- 没有每天自动上传的定时任务。
- 进入“菜单 → 同步”后，才会连接 Wi-Fi 并处理全部待同步笔记。

因此可以在外面全天离线录音，回到家或办公室后再执行一次“同步”。处理顺序为：

1. 优先连接上次成功的 Wi-Fi。
2. 使用硅基流动转写尚未生成 TXT 的 WAV。
3. 将符合条件的文字笔记写入闪念贝壳。
4. 只有手动启用了折叠区域中的旧版 GitHub/Obsidian 功能时，才会继续同步到 GitHub；普通用户可以忽略。
5. 完成后断开 Wi-Fi 并返回待机或菜单。

没有网络、服务超时或 API 调用失败时，原始录音不会被删除。

硅基流动当前规定单次 ASR 文件不超过 50MB、时长不超过 1 小时。按本设备的 WAV 格式，10 分钟约 19.2MB，可以作为一个文件直接转写；超过约 25 分钟的录音仍会完整保存在 microSD 卡，但当前版本暂不自动拆分转写，后续版本可在同步阶段进行不可见的分块处理，最终仍只保留一个 WAV 和一份合并文本。

## 11. 网页管理端

进入“设置 → 传输”后，在浏览器打开屏幕显示的 IP，可以：

- 浏览笔记和转写文字
- 在线播放 WAV
- 下载单条录音或文字
- 导出全部文字
- 删除单条笔记
- 管理标签
- 修改三组 Wi-Fi 和服务密钥
- 测试闪念贝壳连接
- 使用 HTTPS 固件地址执行 OTA 更新

网页只在设备处于“传输模式”时运行。长按录音键退出传输模式。

OTA 页面需要填写可直接下载 `.bin` 文件的 HTTPS 地址；它不是本地文件上传框。更新会写入备用 OTA 分区，成功后自动重启，启动失败时可回滚到上一版本。

## 12. 提示音

进入“设置 → 提示音”，短按录音键依次切换：

```text
关闭 → 水滴 → 经典 → 木质 → 关闭
```

选择会保存在 NVS 中，重启后仍然有效。切换到一个新主题时，设备会立即播放该主题的预览音。提示音包括菜单移动、确认、返回、长录音开始、录音保存、同步成功和删除提示。

- **水滴**：柔和、清脆的下滑水滴声。
- **经典**：清晰的电子按键和双音反馈。
- **木质**：偏低频、短促的木块敲击感。

三个主题使用相同的舒适提示音量。它们不会改变麦克风录音音量，也不会改变录音回放音量。

## 13. 删除和恢复出厂设置

### 删除单条笔记

在笔记详情页长按电源键：

- 短按录音键确认删除。
- 短按电源键或长按录音键取消。

### 全部删除

进入“设置 → 全部删除”：

- “仅设备”：删除 microSD 卡上的设备笔记。
- “设备和 GitHub”：删除设备笔记，并记录待删除的 GitHub 项目，在下次 GitHub 同步时处理。

短按电源键切换选项，短按录音键确认，长按录音键取消。该功能不会自动删除已经写入闪念贝壳的笔记。

### 恢复出厂设置

进入“设置 → 重置”会清除：

- 三组 Wi-Fi
- 硅基流动 API Key
- 闪念贝壳 Token 与开关
- GitHub/Obsidian 配置
- 提示音设置等 NVS 配置

恢复出厂设置不会删除 microSD 卡中的录音和笔记。

## 14. 待机、唤醒和电池

除录音和传输模式外，约 2 分钟无操作后，设备进入深度睡眠。电子墨水屏在断电后仍能保留“记忆面包”待机画面，这是正常现象，并不表示设备仍在高功耗运行。

- 睡眠时按录音键：唤醒并直接开始录音。
- 睡眠时按电源键：唤醒并进入主菜单。
- 电量低于约 15% 时显示低电量提示；恢复到约 20% 以上后允许再次提示。

USB-C 接口可用于固件烧录、串口日志和板载锂电池充电。连接电池前请确认接口类型和正负极与开发板要求一致。

## 15. 中文显示范围

当前字库包含：

- 20,992 个 Unicode 基本区汉字（U+4E00～U+9FFF 全覆盖）
- 21 个常用中文标点
- 英文字母、数字和常用 ASCII 符号
- 不包含日文平假名和片假名

它覆盖日常简体中文、常见繁体字、人名和地名。当前 Memory Bread CJK Bitmap 基于采用 SIL OFL 1.1 的思源黑体 CN Regular 生成；加上中文标点共 21,013 个字形，不包含日文平假名和片假名。

## 16. Flash 和存储空间

开发板具有 8 MB Flash，当前分区如下：

| 分区 | 大小 | 用途 |
|---|---:|---|
| App 0 | 3 MB | 当前运行固件 |
| App 1 | 3 MB | OTA 备用固件 |
| SPIFFS | 约 1.875 MB | 设备内部数据区 |
| 其他 | 约 0.125 MB | NVS、OTA 信息和崩溃记录 |

当前开源构建的 v1.5 固件 BIN 为 2,193,568 字节（约 2.19 MB），占单个 3 MB 应用分区约 69.7%，还剩约 929.8 KiB 应用空间。录音和笔记主要存放在 microSD 卡中，不占用应用固件分区。

当前明确使用的 PSRAM 约 101 KiB：其中约 96 KiB 用作录音环形缓冲区，约 5 KiB 用作墨水屏帧缓冲区。长录音会边录边写入 microSD 卡，因此录制时间变长不会按时长继续占用 PSRAM。录音依赖 PSRAM 缓冲，编译和烧录时必须保留 `PSRAM=opi`。

录音采用边采集、边写入 microSD 的方式，时长增加不会持续占用 PSRAM。当前音频约占 1.92 MB/分钟（约 115 MB/小时）。固件自动读取不同容量存储卡的总容量和剩余空间，因此可使用 8GB、32GB、64GB 或 128GB 等 FAT32 存储卡；64GB、128GB 卡如果出厂为 exFAT，需要先重新格式化为 FAT32。

## 17. USB 烧录

正常更新优先使用网页 OTA。需要 USB 救援或重新烧录时：

1. 拔下 USB。
2. 如果锂电池仍在持续供电，先断开电池，确保主控真正断电。
3. 按住板上丝印为 `BOOT` 的按键。
4. 保持按住并插入 USB 数据线。
5. 等待约 2 秒后松开 BOOT。
6. 在 macOS 上选择出现的 `/dev/tty.usbmodem...` 端口进行烧录。

如果 `/dev/cu.usbmodem...` 可以看到设备但无法连接，可尝试同编号的 `/dev/tty.usbmodem...`。成功日志应包含：

```text
Hash of data verified.
```

普通烧录不会清除 NVS 和 microSD；只有执行整片擦除、恢复出厂设置或手动格式化存储卡才会影响相应数据。

## 18. 常见问题

### 一直显示网络连接失败

进入“设置 → 传输”，等待三组网络全部尝试完毕。连接失败后设备会开启 `MemoryBread-Setup`，连接热点并重新填写 Wi-Fi。确认使用 2.4 GHz 网络，并检查 SSID 和密码大小写。

### 配网页面没有自动打开

连接 `MemoryBread-Setup` 后，手动在浏览器打开 `http://192.168.4.1/provision`。如果手机提示“此网络无法访问互联网”，请选择继续使用该网络，并暂时关闭自动切回蜂窝网络或其他 Wi-Fi 的功能。

### Wi-Fi 扫描没有结果

保持手机连接 `MemoryBread-Setup`，等待几秒后点击“重新扫描”。设备只能扫描 2.4 GHz 网络；隐藏网络需要手动输入 SSID。扫描时热点不会主动关闭。

### 笔记翻页后画面还没有变化

墨水屏刷新有可见延迟，先正常短按并松开，稍等画面完成，不必连续重按。v1.5 会缓存笔记文字并在刷新期间继续接收按键；如果设备长期完全无响应，可正常拔插电源重启，microSD 卡里已经保存的笔记不会因此被删除。

### 长录音结束后突然回到待机，列表里找不到录音

v1.5 已修复超过 2 分钟的长录音结束后被旧待机计时误判的问题。现在 WAV 一关闭就会先加入索引，标签页面只修改分类；开机也会恢复存储卡中未被索引的有效 WAV。若仍出现问题，请先不要格式化 microSD 卡，也不要继续大量录音覆盖编号，保留串口日志和卡内 `/notes` 目录用于排查。

### 录音后没有文字

录音不会自动转写。请先配置 Wi-Fi 和硅基流动 Key，再进入“菜单 → 同步”。

### 闪念贝壳没有出现新笔记

确认已经填写 Token、勾选同步开关，并在联网状态下通过“测试闪念贝壳连接”。随后重新执行“菜单 → 同步”。

### 听不到提示音

在“设置 → 提示音”选择“水滴”“经典”或“木质”，切换时应立即听到预览音。如果仍然听不到，请在笔记详情页测试录音回放，以区分提示音设置和扬声器问题。

### 设备显示存储卡错误

断电后重新插拔 microSD 卡，并确认使用 FAT32。设备没有可用 microSD 卡时无法进入正常录音流程。

### 换线后找不到串口

确认使用支持数据传输的 USB-C 线，而不是仅充电线；尝试直连 Mac 的其他接口，避免不稳定的扩展坞。

## 19. 第一次完整配置检查表

按照以下顺序做一遍，可以最快区分是 Wi-Fi、硅基流动还是闪念贝壳的问题：

1. 插入 FAT32 microSD 卡，先离线录制并回放一条 5～10 秒语音，确认录音硬件正常。
2. 进入“设置 → 传输”，配置至少一组 2.4 GHz Wi-Fi，保存后确认设备获得局域网 IP。
3. 创建一枚新的硅基流动 API Key，将它写入“AI 转写”，保存。
4. 只执行一次设备“同步”，确认测试录音出现中文转写。
5. 在闪念贝壳“设置 → MCP → API 密钥”复制 Token。
6. 将 Token 写入设备，勾选“转写后自动写入闪念贝壳”，保存。
7. 点击“测试连接”。成功后再次执行设备“同步”。
8. 打开闪念贝壳，确认出现标题、摘要、整理正文和原始转写。
9. 删除用于测试的云端笔记，并妥善保存或轮换两枚密钥。

测试时一次只改变一个环节：先验证本地录音，再验证 Wi-Fi，再验证 ASR，最后验证 MCP。这样遇到失败时最容易判断问题出在哪里。
